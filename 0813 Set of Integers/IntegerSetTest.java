/*
 * Create class IntegerSet.  Each IntegerSet object can hold integers in the range 0-100.  The set is represented by an array of booleans.  Array element a[j] is false if integer j is not in the set.  The no argument constructor initializes the array to the "empty set" (i.e., all false values).
 */

public class IntegerSetTest{
	public static void main(String[] args) {
		
	}
}